<?xml version="1.0" encoding="UTF-8"?>
<project default="dist" name="baudiApp" basedir="." xmlns:unless="ant:unless">
    <dirname property="baudiApp.basedir" file="${ant.file.baudiApp}"/>
    
    <!-- import default properties from file -->
    <property file="${baudiApp.basedir}/local.build.properties"/>
    <property file="${baudiApp.basedir}/build.properties"/>
    <tstamp>
        <format property="release.date" pattern="yyyy-MM-dd"/>
    </tstamp>
    <tstamp>
        <format property="release.year" pattern="yyyy"/>
    </tstamp>
    
    <target name="clean-all">
    <description>Delete build and dist directories</description>
        <delete dir="${dist.dir}"/>
        <delete dir="${build.dir}"/>
        <delete dir="${frontend_libs.dir}"/>
    </target>
    
    <target name="clean">
    <description>Start cleaning tasks</description>
        <antcall target="clean-build"/>
        <antcall target="clean-dist"/>
    </target>
    
    <target name="clean-dist" if="${dist.dir}">
        <description>Delete all files in the dist directory</description>
        <delete includeemptydirs="true">
            <fileset dir="${dist.dir}" defaultexcludes="false">
                <include name="**/*"/>
                <exclude name="*.xar"/>
                <exclude name="*.tar.gz"/>
            </fileset>
        </delete>
    </target>
    
    <target name="clean-build" if="${build.dir}">
        <description>Delete all files in the build directory</description>
        <delete includeemptydirs="true">
            <fileset dir="${build.dir}" defaultexcludes="false">
                <include name="**/*"/>
            </fileset>
        </delete>
    </target>
    
    <target name="init" depends="get-current-hash-of-HEAD-short, get-current-hash-of-HEAD, -check-project-status">
        <description>Initialize build and dist directories</description>
        <mkdir dir="${dist.dir}"/>
        <mkdir dir="${build.dir}"/>
    </target>
    
    <target name="get-current-hash-of-HEAD-short">
        <description>Get the shortend hash of the current git HEAD</description>
        <exec executable="${bash.command}" outputproperty="local.revision">
            <arg value="-c"/>
            <arg value="${git.command} rev-parse --short HEAD"/>
            <env key="LANG" value="C"/>
        </exec>
        <echo>Current HEAD: ${local.revision}</echo>
    </target>
    
    <target name="get-current-hash-of-HEAD">
        <description>Get the hash of the current git HEAD</description>
        <exec executable="${bash.command}" outputproperty="local.revision.full">
            <arg value="-c"/>
            <arg value="${git.command} rev-parse HEAD"/>
            <env key="LANG" value="C"/>
        </exec>
        <echo>Current HEAD: ${local.revision.full}</echo>
    </target>
    
    <target name="-check-project-status" unless="${project.status}">
        <property name="project.status" value="production"/>
    </target>
    
    <target name="-set-status-flags" depends="-check-project-status">
        <condition property="project.status.is.development">
            <equals arg1="${project.status}" arg2="development"/>
        </condition>
    </target>
    
    <target name="yarn" depends="init">
        <description>Update javascript libraries via yarn</description>
        <exec executable="${yarn.cmd}" failonerror="yes">
            <arg line="install --check-files"/>
        </exec>
    </target>
    
    <target name="sass-dev" depends="init" if="project.status.is.development">
        <description>Create CSS files via sass (development mode with sourcemaps)</description>
        <exec executable="${sass.cmd}" failonerror="yes">
            <arg line="--style expanded --source-map --silence-deprecation=import,color-functions,global-builtin,abs-percent resources/sass/main.scss ${dist.dir}/resources/css/main.css"/>
        </exec>
    </target>
    
    <target name="sass-prod" depends="init" unless="project.status.is.development">
        <description>Create CSS files via sass (production mode compressed)</description>
        <exec executable="${sass.cmd}" failonerror="yes">
            <arg line="--style compressed --no-source-map --silence-deprecation=import,color-functions,global-builtin,abs-percent resources/sass/main.scss ${dist.dir}/resources/css/main.css"/>
        </exec>
    </target>
    
    <target name="sass" depends="-set-status-flags, sass-dev, sass-prod">
        <description>Create CSS files via sass (calls dev or prod target based on project.status)</description>
    </target>

    <target name="dist" depends="init, yarn, sass">
        <description>Build the distribution package</description>
        <property name="project.app.pkg.name" value="${project.app}-${project.version}-${local.revision}.xar"/>
        <!-- Copy files to build directory-->
        <copy todir="${build.dir}/">
            <fileset dir=".">
                <include name="**/*.*"/>
                <exclude name="build.xml"/>
                <exclude name="*.code-workspace"/>
                <exclude name=".git*"/>
                <exclude name=".github/"/>
                <exclude name="*.tmpl"/>
                <exclude name="*.xpr"/>
                <exclude name="*.code-workspace"/>
                <exclude name="**/.DS_Store"/>
                <exclude name="*.properties"/>
                <exclude name="${build.dir}/**"/>
                <exclude name="${dist.dir}/**"/>
                <exclude name="scripts/**"/>
                <exclude name="saxon-*.jar"/>
                <exclude name="doc/"/>
                <exclude name="notices/"/>
                <exclude name="blob/"/>
                <exclude name="**/__contents__.xml"/>
                <exclude name="node_modules/**"/>
                <exclude name="libs/**"/>
                <exclude name="resources/sass/**"/>
                <exclude name="resources/lib/**"/>
                <exclude name="package.json"/>
                <exclude name="package-lock.json"/>
                <exclude name="yarn.lock"/>
                <exclude name="watch-sass.sh"/>
                <exclude name="*.md"/>
                <exclude name="saxon*.jar"/>
            </fileset>
        </copy>
        
        <!-- Copy libs from ${frontend_libs.dir} -->
        <copy todir="${build.dir}/resources/lib" failonerror="false">
            <fileset dir="${frontend_libs.dir}">
                <include name="**/*.js"/>
                <include name="**/*.css"/>
                <include name="**/*.woff"/>
                <include name="**/*.woff2"/>
                <include name="**/*.ttf"/>
                <include name="**/*.eot"/>
                <include name="**/*.svg"/>
                <include name="**/*.png"/>
                <include name="**/*.jpg"/>
                <include name="**/*.gif"/>
                <!-- exclude unnecessary directories -->
                <exclude name="**/node_modules/"/>
                <exclude name="**/test/**"/>
                <exclude name="**/tests/**"/>
                <exclude name="**/docs/**"/>
                <exclude name="**/examples/**"/>
                <exclude name="**/*.html"/>
                <exclude name="saxon*.jar"/>
            </fileset>
        </copy>
        
        <copy file="expath-pkg.xml.tmpl" tofile="${build.dir}/expath-pkg.xml" filtering="true" overwrite="true">
        <!-- Create expath-pkg.xml -->
            <filterset>
                <filter token="project.app" value="${project.app}"/>
                <filter token="project.name" value="${project.name}"/>
                <filter token="project.version" value="${project.version}"/>
                <filter token="project.version.hash" value="${local.revision}"/>
                <filter token="project.url" value="${project.url}"/>
            </filterset>
        </copy>

        <copy file="repo.xml.tmpl" tofile="${build.dir}/repo.xml" filtering="true" overwrite="true">
        <!-- Create repo.xml -->
            <filterset>
                <filter token="data.target" value="${project.app}"/>
                <filter token="project.name" value="${project.name}"/>
                <filter token="project.status" value="${project.status}"/>
            </filterset>
        </copy>
        
        <copy file="CITATION.cff.tmpl" tofile="${build.dir}/CITATION.cff" filtering="true" overwrite="true">
        <!-- Create CITATION.cff -->
            <filterset>
                <filter token="project.app" value="${project.app}"/>
                <filter token="project.version" value="${project.version}"/>
                <filter token="project.version.hash" value="${local.revision.full}"/>
                <filter token="project.url" value="${project.url}"/>
                <filter token="release.date" value="${release.date}"/>
            </filterset>
        </copy>

        <!-- Create .xar package by zipping the date in ${build.dir} -->
        <zip destfile="${dist.dir}/${project.app.pkg.name}">
            <fileset dir="${build.dir}/">
                <include name="**/*.*"/>
                <exclude name="${dist.dir}/"/>
            </fileset>
        </zip>

        <!-- Cleaning -->
        <delete dir="${build.dir}"/>
    </target>
    
    <!-- Copy xar package to baudi-docker repo (autodeploy) -->
    <target name="update-baudi-docker" depends="dist">
    <description>Copy the .xar package to the baudi-docker repo (locally)</description>
        <echo>Delete old packages</echo>
        <delete>
            <fileset dir="./../baudi-docker/autodeploy">
                <include name="${project.app}-*.xar"/>
            </fileset>
        </delete>
        <echo>Add new package</echo>
        <copy file="${dist.dir}/${project.app.pkg.name}" todir="./../baudi-docker/autodeploy" overwrite="yes"/>
    </target>
    
</project>
