#
# Don't directly modify this file.
# Instead, copy it to local.build.properties and edit that.
#
# general
project.app=baudiApp
project.name=Baumann-Digital Portal App
project.url=https://github.com/Baumann-Digital/portal-app
project.version=1.1.1

# build output dirs
build.dir=build
dist.dir=dist

# frontend libs
frontend_libs.dir=libs
dev_libs.dir=node_modules

# engines
bash.command=/bin/bash
git.command=/usr/bin/git
yarn.cmd=yarn
sass.cmd=${dev_libs.dir}/.bin/sass
